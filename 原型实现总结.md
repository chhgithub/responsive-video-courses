# 视频课程管理系统原型实现总结

## 项目概述

基于 Vue 3 + TypeScript + Vben Admin + Ant Design Vue 技术栈实现的响应式视频课程管理系统原型，包含完整的**前台展示**和**后台管理**功能，使用 mock 数据进行演示。

## 已实现的功能模块

### 一、前台模块（响应式设计，适配PC/Pad/手机端）

#### 1. 首页展示模块 (`/portal`)

- **Banner轮播图**：自动轮播，支持手动切换
- **精选课程**：网格布局展示课程卡片
- **资讯公告/活动日历/热点聚合**：Tab切换展示
- **课程统计气泡图**：可视化展示课程数据
- **在线咨询表单**：用户可提交咨询问题

#### 2. 课程中心模块

- **课程列表页** (`/portal/courses`)：支持按类型筛选（微课程/公益课程/付费课程/科研赋能/K12集训/成人集训）
- **课程详情页** (`/portal/course/:id`)：显示课程信息、章节列表、权限控制
- **视频播放器** (`/portal/player/:courseId/:chapterId`)：支持试看、进度保存
- **专项课程页面**：微课程、公益课程、科研赋能、集训计划

#### 3. 会员管理模块

- **登录/注册页面**：`/auth/login`、`/auth/register`
- **个人中心** (`/member/profile`)：显示会员信息、学习统计
- **我的课程** (`/member/courses`)：显示已购课程和学习进度
- **订单记录** (`/member/orders`)：显示订单历史
- **账号设置** (`/member/settings`)：编辑个人信息

#### 4. 支付模块

- **订单确认页** (`/payment/checkout/:courseId`)：
  - 订单信息确认
  - 支付方式选择（微信/支付宝）
  - 支付结果展示
  - 支付成功后自动解锁权限

### 二、后台管理系统模块

#### 1. 课程管理（已增强）

- **扩展的字段**：
  - 课程类型（微课程/公益/付费/科研赋能/K12集训/成人集训）
  - 试看设置（是否可试看、试看时长）
  - 有效期（天数为单位）
  - 难度等级（初级/中级/高级）
  - 标签支持
- **章节管理**：树形结构，支持添加/删除/设置免费

#### 2. 富文本编辑器管理

- 基于 TinyMCE 的富文本编辑器
- 支持多种内容类型编辑（课程介绍、认证中心、关于我们、师资介绍）
- 草稿/发布状态管理
- 快捷编辑按钮

#### 3. 菜单管理（已扩展）

- **新增字段**：
  - `scope`：菜单范围（后台管理/前台展示）
  - `responsive`：响应式显示配置（移动端/平板/桌面）
- 支持完整的菜单CRUD操作

#### 4. 用户管理

- 用户列表查询
- 用户增删改查
- 密码重置功能
- 部门树选择
- 角色授权

#### 5. 字典管理

- **业务字典常量**：`/src/constants/dict.ts`
- **字典数据Mock**：`/src/api/system/dict/mock-data.ts`
- **支持的字典类型**：
  - 课程相关：难度、状态、类型、等级
  - 会员相关：等级、状态
  - 订单相关：状态、支付方式、支付状态
- **字典使用示例**：`/src/components/dict/DictExample.vue`

## 技术架构

### 前端技术栈

- **框架**：Vue 3.5+ (Composition API)
- **UI组件**：Ant Design Vue 4.x + Vben Admin 组件
- **状态管理**：Pinia（支持持久化）
- **路由**：Vue Router
- **HTTP客户端**：Axios（已封装）
- **富文本编辑**：TinyMCE
- **响应式**：Tailwind CSS + Ant Design Grid
- **表格**：VxeGrid
- **表单**：VbenForm（基于 zod 验证）

### 项目结构

```
apps/web-antd/src/
├── api/                    # API接口
│   ├── course/             # 课程相关API
│   ├── system/             # 系统管理API
│   └── ...
├── components/             # 公共组件
│   ├── dict/               # 字典组件
│   ├── tinymce/            # 富文本编辑器
│   └── upload/             # 上传组件
├── constants/              # 常量定义
│   └── dict.ts             # 字典常量
├── layouts/                # 布局组件
│   ├── basic.vue           # 后台布局
│   ├── auth.vue            # 认证布局
│   └── portal.vue          # 前台布局（新增）
├── router/                 # 路由配置
│   └── routes/modules/
│       └── portal.ts       # 前台路由（新增）
├── store/                  # 状态管理
│   └── dict.ts             # 字典Store
├── views/                  # 页面视图
│   ├── portal/             # 前台页面（新增）
│   │   ├── index.vue       # 首页
│   │   ├── courses.vue     # 课程中心
│   │   ├── course-detail.vue # 课程详情
│   │   ├── player.vue      # 视频播放器
│   │   └── components/     # 前台组件
│   ├── member/             # 会员模块（新增）
│   │   └── profile/        # 个人中心
│   ├── payment/            # 支付模块（新增）
│   ├── course/             # 课程管理（已增强）
│   ├── introduction/       # 介绍信息管理
│   └── system/             # 系统管理
│       ├── user/           # 用户管理
│       ├── menu/           # 菜单管理（已扩展）
│       └── dict/           # 字典管理
└── utils/                  # 工具函数
```

## 使用说明

### 启动项目

1. 安装依赖：

```bash
pnpm install
```

2. 启动开发服务器：

```bash
pnpm run dev
```

3. 在浏览器中访问：
   - 后台管理系统：http://localhost:5173
   - 前台首页：http://localhost:5173/portal

### 访问路径

#### 前台路径

- `/portal` - 首页
- `/portal/courses` - 课程中心
- `/portal/course/:id` - 课程详情
- `/portal/player/:courseId/:chapterId` - 视频播放
- `/portal/micro` - 微课程
- `/portal/public` - 公益课程
- `/portal/research` - 科研赋能
- `/portal/training` - 集训计划
- `/member/profile` - 个人中心
- `/member/courses` - 我的课程
- `/member/orders` - 订单记录
- `/payment/checkout/:courseId` - 支付页面

#### 后台路径

- `/dashboard` - 仪表盘
- `/course/list` - 课程列表
- `/introduction/list` - 介绍信息管理
- `/system/user` - 用户管理
- `/system/menu` - 菜单管理
- `/system/dict` - 字典管理

### Mock数据

项目使用 Mock 数据进行演示，数据位置：

- 课程API：`/src/api/course/index.ts`
- 字典数据：`/src/api/system/dict/mock-data.ts`

### 响应式设计

前台所有页面均采用响应式设计：

- **手机端**（< 768px）：单列布局、抽屉菜单
- **平板端**（768px - 1023px）：两列布局
- **桌面端**（≥ 1024px）：多列布局、完整导航

## 关键文件说明

### 前台核心文件

- `/src/layouts/portal.vue` - 前台布局（响应式导航）
- `/src/views/portal/index.vue` - 首页
- `/src/views/portal/components/CourseCard.vue` - 课程卡片组件（高复用）

### 后台增强文件

- `/src/views/course/course-drawer.vue` - 课程编辑表单（已扩展字段）
- `/src/views/course/chapter-manager.vue` - 章节管理组件（新增）
- `/src/views/system/user/member-courses-modal.vue` - 会员课程查看（新增）
- `/src/views/system/menu/data.tsx` - 菜单表单配置（已扩展）

### 字典管理文件

- `/src/constants/dict.ts` - 字典常量定义
- `/src/api/system/dict/mock-data.ts` - 字典初始数据
- `/src/components/dict/DictExample.vue` - 字典使用示例

## 扩展数据模型

### 课程模型扩展

```typescript
interface Course {
  // ... 原有字段
  courseType?: CourseType; // 课程类型
  isTrial?: boolean; // 是否可试看
  trialDuration?: number; // 试看时长(秒)
  validDays?: number; // 有效期(天)
  difficulty?: Difficulty; // 难度等级
  tags?: string[]; // 标签
  totalDuration?: number; // 总时长
  progress?: number; // 学习进度
}
```

### 菜单模型扩展

```typescript
interface Menu {
  // ... 原有字段
  scope?: 'admin' | 'portal'; // 前台/后台标识
  responsive?: string[]; // 响应式配置
}
```

## 后续开发建议

1. **后端接口对接**：将 Mock API 替换为真实的后端接口
2. **权限控制**：实现基于角色的访问控制（RBAC）
3. **支付对接**：对接微信支付和支付宝支付
4. **视频上传**：对接云存储服务（阿里云OSS、腾讯云COS等）
5. **实时通信**：使用 WebSocket 实现实时消息推送
6. **数据分析**：添加学习数据分析和统计功能

## 技术亮点

- ✅ 完整的前后台分离架构
- ✅ 响应式设计，适配多种设备
- ✅ 组件化开发，代码复用率高
- ✅ TypeScript 类型安全
- ✅ Pinia 状态管理 + 持久化
- ✅ Vben Admin 成熟的后台管理模板
- ✅ Ant Design Vue 企业级 UI 组件
- ✅ Mock 数据驱动，方便原型演示
